def solution(n):
    '''n이 양의 정수 x의 제곱이라면 x+1의 제곱을 리턴하고,
    n이 양의 정수 x의 제곱이 아니라면 -1을 리턴하는 함수'''

    if (n**0.5)**2 == int((n**0.5))**2:
        # 제곱수 일 경우
        return ((n**0.5)+1)**2
    else:
        return -1